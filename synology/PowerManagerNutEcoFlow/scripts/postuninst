#!/bin/bash
# Post-uninstall script cleans runtime directories after removal.
set -euo pipefail

readonly TARGET_ROOT="/var/packages/PowerManagerNutEcoFlow"
readonly TARGET_DIR="${TARGET_ROOT}/target"

if [ "${SYNOPKG_PKG_STATUS}" = "UNINSTALL" ]; then
    rm -rf "${TARGET_DIR}"
fi

exit 0
